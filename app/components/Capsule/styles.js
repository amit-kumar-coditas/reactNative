import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  containerStyle: {
    borderWidth: 1,
    borderColor: '#FFFFFF',
    borderRadius: 25,
    padding: 6.5,
  },
  label: {fontSize: 12, color: '#FFFFFF', fontWeight: 'bold'},
});

export default styles;
